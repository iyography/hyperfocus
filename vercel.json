{
  "buildCommand": "pnpm --filter @hyperfocus/web build",
  "outputDirectory": "web/dist",
  "installCommand": "pnpm install",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
